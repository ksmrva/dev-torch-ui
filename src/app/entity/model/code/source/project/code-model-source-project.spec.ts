import { CodeModelSourceProject } from './code-model-source-project';

describe('CodeModelSourceProject', () => {
  it('should create an instance', () => {
    expect(new CodeModelSourceProject()).toBeTruthy();
  });
});
