import { CodeModelSourceFileCodeExtension } from './code-model-source-file-code-extension';

describe('CodeModelSourceFileCodeExtension', () => {
  it('should create an instance', () => {
    expect(new CodeModelSourceFileCodeExtension()).toBeTruthy();
  });
});
