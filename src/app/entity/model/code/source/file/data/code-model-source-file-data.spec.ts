import { CodeModelSourceFileData } from './code-model-source-file-data';

describe('CodeModelSourceFileData', () => {
  it('should create an instance', () => {
    expect(new CodeModelSourceFileData()).toBeTruthy();
  });
});
