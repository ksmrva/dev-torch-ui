import { SqlDatabaseDetailPath } from "./sql-database-path";

describe("SqlDatabaseDetailPath", () => {
  it("should create an instance", () => {
    expect(new SqlDatabaseDetailPath()).toBeTruthy();
  });
});
