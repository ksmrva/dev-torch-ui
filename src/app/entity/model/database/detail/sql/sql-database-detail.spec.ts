import { SqlDatabaseDetail } from "./sql-database-detail";

describe("SqlDatabaseDetail", () => {
  it("should create an instance", () => {
    expect(new SqlDatabaseDetail()).toBeTruthy();
  });
});
