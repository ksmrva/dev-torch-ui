import { DbCollectionCategory } from "./db-collection-category";

describe("DbCollectionCategory", () => {
  it("should create an instance", () => {
    expect(new DbCollectionCategory()).toBeTruthy();
  });
});
