import { DbForeignKeyModel } from "./db-foreign-key-model";

describe("DbForeignKeyModel", () => {
  it("should create an instance", () => {
    expect(new DbForeignKeyModel()).toBeTruthy();
  });
});
