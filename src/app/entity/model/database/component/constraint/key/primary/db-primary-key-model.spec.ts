import { DbPrimaryKeyModel } from "./db-primary-key-model";

describe("DbPrimaryKeyModel", () => {
  it("should create an instance", () => {
    expect(new DbPrimaryKeyModel()).toBeTruthy();
  });
});
