import { DbModelSourceConfig } from "./db-model-source-config";

describe("DbModelSourceConfig", () => {
  it("should create an instance", () => {
    expect(new DbModelSourceConfig()).toBeTruthy();
  });
});
