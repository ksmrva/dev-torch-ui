import { DbModelSourceSupportedDriver } from './db-model-source-supported-driver';

describe('DbModelSourceSupportedDriver', () => {
  it('should create an instance', () => {
    expect(new DbModelSourceSupportedDriver()).toBeTruthy();
  });
});
