import { DbModelSourceUrlSupportedScheme } from './db-model-source-url-supported-scheme';

describe('DbModelSourceUrlSupportedScheme', () => {
  it('should create an instance', () => {
    expect(new DbModelSourceUrlSupportedScheme()).toBeTruthy();
  });
});
