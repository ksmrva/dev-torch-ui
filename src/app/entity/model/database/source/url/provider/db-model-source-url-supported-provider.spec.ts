import { DbModelSourceUrlSupportedProvider } from './db-model-source-url-supported-provider';

describe('DbModelSourceUrlSupportedProvider', () => {
  it('should create an instance', () => {
    expect(new DbModelSourceUrlSupportedProvider()).toBeTruthy();
  });
});
