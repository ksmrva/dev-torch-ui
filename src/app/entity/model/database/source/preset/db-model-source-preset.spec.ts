import { DbModelSourcePreset } from './db-model-source-preset';

describe('DbModelSourcePreset', () => {
  it('should create an instance', () => {
    expect(new DbModelSourcePreset()).toBeTruthy();
  });
});
