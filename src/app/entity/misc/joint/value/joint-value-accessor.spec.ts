import { JointValueAccessor } from "./joint-value-accessor";

describe("JointValueAccessor", () => {
  it("should create an instance", () => {
    expect(new JointValueAccessor()).toBeTruthy();
  });
});
