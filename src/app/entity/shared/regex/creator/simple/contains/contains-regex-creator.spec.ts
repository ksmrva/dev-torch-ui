import { ContainsRegexCreator } from "./contains-regex-creator";

describe("ContainsRegexCreator", () => {
  it("should create an instance", () => {
    expect(new ContainsRegexCreator()).toBeTruthy();
  });
});
