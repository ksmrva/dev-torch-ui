import { ExplorerPanelListEntry } from './explorer-panel-list-entry';

describe('ExplorerPanelListEntry', () => {
  it('should create an instance', () => {
    expect(new ExplorerPanelListEntry()).toBeTruthy();
  });
});
